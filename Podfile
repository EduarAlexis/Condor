# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'pruebaCondor' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for pruebaCondor
  pod 'Alamofire', '~> 5.0.0-rc.3'
  pod 'SwiftSpinner'
  pod 'SkeletonView'
  pod 'PINRemoteImage'

  target 'pruebaCondorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'pruebaCondorUITests' do
    # Pods for testing
  end

end
